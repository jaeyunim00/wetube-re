extends base.pug

block content
  if errorMessage 
    span=errorMessage
  form(method="post")
    input(type="text", name="username" placeholder = "닉네임")
    input(type="password", name="password" placeholder = "패스워드")
    input(placeholder="", type="submit", value="로그인")
    br
    a(href="/oAuth/github/start") 깃허브로 로그인 &rarr;
    br
    a(href="/oAuth/kakao/start") 카카오로 로그인 &rarr;
  hr
  div
    span 계정이 없으세요?
    a(href="/join") 계정 생성하기 &rarr;
