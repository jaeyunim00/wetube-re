extends ../base.pug

block content
  if errorMessage
    span=errorMessage
  form(method="post")
    input(type="password", placeholder="기존 비밀번호", name = "password_old")
    input(type="password", placeholder="새로운 비밀번호", name="password_new")
    input(type="password", placeholder="새로운 비밀번호 (다시입력)", name="password_new_confirm")
    input(type="submit", value="비밀번호 변경")