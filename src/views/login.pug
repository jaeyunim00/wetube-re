extends base.pug

block content
  if errorMessage 
    span=errorMessage
  form(method="post")
    input(type="text", name="username" placeholder = "닉네임")
    input(type="password", name="password" placeholder = "패스워드")
    input(placeholder="", type="submit", value="로그인")